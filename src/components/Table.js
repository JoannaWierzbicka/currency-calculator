/* eslint-disable react/prop-types */
import React from 'react'

export const Table = (props) => {
  const { data } = props
  console.log(data)
  return (
    <table></table>
  )
}

export default Table
